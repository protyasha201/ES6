const firstName = "James";
const lastName = "Hetfield";
const fullName = firstName + " " + lastName;
const fullName2 = `${firstName} ${lastName} is a good boy`; // `` - these are used to easily make template. ${} -whatever we want to display variable between {}. 
console.log(fullName2);

const multiline = "I am an athiest\nAnd I am so happy";
console.log(multiline);


const multiline2 = `we are great
we are human
we are forever`;
console.log(multiline2); // we can use multi lines using ``